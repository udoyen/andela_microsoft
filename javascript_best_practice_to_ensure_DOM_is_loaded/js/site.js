// To ensure the DOM is ready for use always start you scripts from an init() function

function init(evt) {
    // Run your codes inside this function
}

// Another method would be

window.onload = init2;

function init2(evt) {
    // run code inside here!
}
